"use client"

import { Separator } from "@chatbotx.io/ui/components/ui/separator"

type EmailLineStepEditorProps = {
  parentName: string
}

export default function EmailLineStepEditor(_props: EmailLineStepEditorProps) {
  return <Separator />
}
